# Fixed eta ATP run — 2026-07-11

## Цель

Проверить, выводится ли фиксированная eta-цель

```text
U*Ib=r  =>  A=Ib*c=z
```

из E677, левого сокращения и строгой period-3 постановки.

Для автоматического доказателя основная цель записана как:

```text
A = z
```

## Запуск

GitHub Actions:

`https://github.com/Grisha-Pochuev/finite-magma-e677-to-e255/actions/runs/29168947710`

Run ID:

`29168947710`

Коммит запуска:

`6f2f8931f17f59a9989ebd784a243af12d40f542`

## Планировавшаяся конфигурация

- 10 заданий E prover;
- 10 заданий Vampire;
- пять вариантов постановки: `base`, `row-image`, `expanded`, `contradiction`, `ground-boost`;
- до четырех ядер на задание;
- до 5 часов 40 минут поиска внутри задания.

## Файлы

- `problem.p` — исходная TPTP-постановка;
- `run_one.sh` — точный исторический сценарий одного задания;
- `workflow-used.yml` — точная копия использованного сценария GitHub Actions;
- `RESULTS.md` — проверенный итог и разбор технических сбоев.

## Важное предупреждение

Исторические `run_one.sh` и `workflow-used.yml` сохраняются для воспроизводимости, но их нельзя запускать повторно без исправления.

Обнаружены как минимум две ошибки:

1. `casc_sat` использовался как значение `--mode`, хотя текущий Vampire требует `--mode casc --intent sat`.
2. Сценарий завершался безусловным `exit 0`, из-за чего часть технически ошибочных заданий выглядела успешной.

Перед следующим большим прогоном нужен отдельный короткий сквозной тест.
